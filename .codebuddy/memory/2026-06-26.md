# 2026-06-26

- **修复"点击下拉菜单没反应"问题**：
  1. `public/js/index.js` 和 `public-test/js/index.js` 中 `toggleAnnouncementBar()` 被重复定义两次（两次分别 toggle `collapsed` 和 `hidden`），第二个覆盖第一个。合并为一个函数，只 toggle `hidden`。
  2. v4-linear `public-test/v4-linear/index.html` 中 `#announcementBar` 同时有 inline `onclick="toggleAnnouncementExpand()"` 和 JS `addEventListener('click', toggleAnnouncementExpand)`，导致函数被调用两次——面板打开后立即关闭，表现如"没反应"。移除 inline onclick，保留 addEventListener。

- **公告栏重做：采用 design-1-dropdown.html 内联下拉方案（public/ 和 public-test/v4-linear/ 均已完成）**：
  - 彻底删除旧的 `position: fixed` 三元素架构（backdrop/expanded/bar），替换为简洁的 `position: relative` wrapper + `position: absolute` dropdown
  - **HTML**：`.ann-bar-wrap`(relative) → `.ann-bar`(正常流) + `.ann-dropdown`(absolute, top:100%)
  - **CSS**：公告条在正常文档流，下拉面板带三角箭头 + dropIn 动画（scaleY+translateY），列表项首条圆点加黑突出，每项有删除按钮
  - **JS (production)**：`toggleAnnDropdown()` + `closeAnnDropdown()` + 全局点击外部关闭 + `updateAnnBar()` + `renderDropdown()`，保留 Modal 新增公告
  - **JS (v4-linear 测试版)**：同上dropdown架构，但保留内联编辑功能（`showAddAnnouncement()`在dropdown顶部插入表单、`saveNewAnnouncement()`、`deleteAnnouncement()`），删除了旧的 `toggleAnnouncementExpand()`、`startEditAnnouncement()`、`saveEditAnnouncement()`、`loadAnnouncementsForExpand()`
  - 多个 `announcementsData` 变量替代旧的 `announcements` 或直接 fetch，统一管理数据
  - 不再使用 `.hidden` 类、不再依赖 Bootstrap hidden 类、不再使用任何 fixed 定位

- **公告栏 UI 细化（public-test/v4-linear/）**：
  - **下拉面板毛玻璃**：`background: rgba(255,255,255,0.70)`, `backdrop-filter: blur(24px) saturate(180%)`，宽度与 annBarWrap 一致（`left:0; right:0`）
  - **内容变暗遮罩**：`.app-container:has(.ann-bar.open)::after` 覆盖全页面，`annBarWrap` 用 `z-index: 101` 浮在暗色之上
  - **文本翻转**：`.ann-bar-text-slot` 含两层层叠文本，展开时"系统公告"滑入、公告内容滑出；收起时反向
  - **Badge 变形动画**：badge 收起时为计数小圆角（如 "3"），展开时撑大变为 "+ 新增" 按钮（`border-radius: 10→6px`, `height: 20→28px`, `background: 深灰→primary`, 文字缩放切换）；删除了独立的 `.ann-bar-add-btn` 元素；JS 中 `updateAnnBar()` 不再隐藏 badge（改为显示 "0"），新增 `handleBadgeClick()` 处理 badge 在展开状态下的点击（stopPropagation + showAddAnnouncement）
  - **修复全局字体变粗**：`style.css` 第1行开头有 `你这个` 三个中文字符（无效CSS语法），导致整个样式表解析失败，页面回退浏览器默认样式。移除后恢复。Badge 布局恢复原始 `text-align:center; line-height:20px` 模型，仅增加 `position:relative; overflow:hidden`；子选择器加 `.ann-bar` 前缀防全局污染。
  - **公告编辑框美化**：改为底划线极简风格（`border: none; border-bottom: 1px solid #e8e8e8`），`background: transparent`，`border-radius: 0`，聚焦仅底划线变黑。
  - **公告圆点不可选中**：`.ann-dropdown-item .bullet` 加 `user-select: none; pointer-events: none`。
  - **生产版前端更新**：`public/` → 备份 `public-old/`（`/old` 路由），v4-linear 文件复制到 `public/` 成为新版生产前端。修复 `index.html` 和 `device.html` 中 `../css/bootstrap-icons.css` → `css/bootstrap-icons.css`。生产版地址不变：`http://localhost:3001/`，旧版备份：`http://localhost:3001/old/`。
