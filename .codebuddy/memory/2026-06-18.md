# 2026-06-18

- 09:24: **README 快速开始重构成"访问"和"开发"两部分** —— 原"云端使用（直接访问）"→"访问"（面向普通用户，直接打开网页即可）；原"本地开发"→"开发"（面向开发者，含克隆、安装、数据库/S3 切换配置）。修改 README.md。
- 11:20: **README 数据库/S3 配置改为通用示例** —— 去掉 TiDB、hi168、cstcloud 等具体平台/仓库名，改为 cloud/local、config1/config2 等通用占位符。
- 11:40: **清空 devices 表** —— TiDB Cloud 上 14 台设备全部删除，自增 ID 重置，准备重新录入。
- 11:48: **新增"清空当前仓库"功能** —— 在添加设备按钮的下拉菜单中增加"清空当前仓库"按钮（桌面端+移动端），后端新增 `POST /api/devices/clear-warehouse`（server + worker），前端双重确认弹窗防止误操作。支持清空指定仓库或总仓库（全部设备）。
- 12:38: **优化批量粘贴 AI 提示语** —— name 字段说明明确"只填物品本身，不写品牌/型号"，spec_model 说明改为"品牌、型号、容量等信息放这里"。同时更新 prompt 示例和 JSON 示例：U盘→name:"U盘" spec_model:"金士顿 64G"，显示器→name:"显示器" spec_model:"Dell U2723QE"。
- 15:54: **Worker import-batch 404 排查** —— worker/index.js 代码已正确写入但未部署到 Cloudflare Pages，git status 显示未 staged，需 commit + push 后自动部署。
- 16:00: **未指定仓库设备ID标红** —— 表格视图和卡片视图中，`warehouse_name` 为空/NULL 的设备其 `.device-id-badge` 附加 `.device-id-badge-no-warehouse` 样式（红色边框+红色文字+浅红背景），同时添加 `title="未指定仓库"` 提示。CSS 新增对应样式。
- 16:13: **AI 提示语增加 warehouse_name 识别** —— 字段说明新增 `warehouse_name: 从描述中推断仓库名（如"工作仓库的笔记本"→工作仓库）`，去掉"不需要填 warehouse_name"的限制。示例中增补带 warehouse_name 的条目。
- 16:18: **import-batch 仓库不存在校验** —— 入库前查 `warehouses` 表校验 warehouse_name 是否存在，不存在则报错「第N行: 仓库「xxx」不存在」并跳过该设备。server 和 worker 均已添加。
- 16:52: **import-batch 改为整体拦截** —— 校验阶段有任何错误（名称缺失、仓库不存在、ID重复等），整批不执行插入，返回全部错误。只有零错误才入库。server 和 worker 均已修改。
