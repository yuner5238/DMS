# 2026-06-29

- **补全 4 个新版缺失功能**（从旧版迁移到 v4-linear/public/）：
  1. **批量编号补全**：`backfillDeviceCodes()` 调用 `POST /api/devices/backfill-codes`，按钮 `#btnBackfillCodes` 在工具栏（sort/import/export 之后）。`updateBackfillBtnVisibility()` 检查 `allDevices` 中是否存在 `!d.device_id && !d.deviceId` 的设备，仅当有缺失 ID 时才显示按钮（而非有设备就显示）。在 `renderDevices()` 末尾调用。
  2. **清空仓库**：`clearCurrentWarehouse()` 调用 `POST /api/devices/clear-warehouse`，入口在"添加设备"按钮右侧 split 下拉菜单。支持清空选中仓库和"全部"仓库（`currentWarehouse` 为 null 时传 `warehouseName: null`，后端清空全部设备）。
  3. **仓库列表"显示更多"**：`renderWarehouseList()` 初始显示 5 个，`warehouseShowAll` 变量控制，`#warehouseShowMore` 按钮切换。
  4. **侧边栏展开/收缩**：PC 端已移除所有收缩按钮（`.sidebar-toggle-pc`、`.sidebar.collapsed`、`toggleSidebarPC()`、Ctrl+\ 快捷键）。仅保留移动端 `toggleSidebar()`（overlay + `show` class）和 `.sidebar-toggle-btn` hamburger 按钮。

- **创建 5 个侧边栏设计方案测试页**（`public/test-sidebar-1~5.html`）：方案一（汉堡+迷你侧边栏三级循环）、方案二（Notion 悬停触发 + overlay 弹出）、方案三（GitHub 底部固定按钮二级折叠）、方案四（VS Code 快捷键 Ctrl+\ + 工具栏原位切换）、方案五（拖拽边缘 180-400px + 双击折叠）。

- **导入 3 个示例设备**（全字段补全）到仓库"工作"：
  - 000004 联想ThinkPad X1 Carbon | 到期 +6天 (2026-07-05) | 负责人张伟
  - 000005 华为AX6路由器 | 到期 +29天 (2026-07-28) | 负责人李娜
  - 000006 佳能激光打印机 | 到期 +200天 (2027-01-15) | 负责人王芳

- **表格视图列宽优化**：将 `nth-child()` 列宽规则替换为 `[data-col]` 属性选择器，解决动态列顺序下列宽不对应的问题。设备ID改为 `width: 1%; white-space: nowrap` 自适应收缩，名称列 `width: auto` 撑满，短列自动适配。
- **表格列宽精细调整**：为每列设置合适 px 宽度（设备ID 95px、序列号 130px、规格型号 145px、来源 85px、数量 56px、标签 130px、部门 140px、负责人 80px、位置 115px、状态 68px、到期 95px、入库 105px、备注 48px、操作 112px），名称列 min-width 160px 自动撑满。固定宽度列添加 `text-overflow: ellipsis` 溢出截断。

- **响应式表格列宽 + 自动切换视图**：
  - 表格改用 `table-layout: fixed`，所有列改为百分比宽度。protected 列（ID/数量/标签/到期）加 `min-width` 保证内容不截断，非 protected 列纯百分比同比例缩放。
  - `<1600px` 宽度时标签列只显示 1 个标签 + "+n" 折叠（`renderTagBadges` 新增 `maxVisible` 参数）。
  - `<1366px` 自动切换到卡片视图（`resize` 监听 200ms 防抖），用户手动切换后 `autoSwitchedToCard` 标记阻止反向自动切换。初始化时也检查宽度。

- **4步渐进式压缩策略**（重构 `applyViewByWidth()`）：
  - Step1（CSS自动）：全部列同步压缩，ID(86px)/数量(50px)/到期(90px)/备注(42px) 达到 min-width 后停止
  - Step2（≤1500px JS+CSS）：标签列只显示1个+ "+n"，标签列 min-width 从 130px 缩减到 60px
  - Step3（≤1200px JS）：自动切换卡片视图；恢复宽度后自动切回表格（仅当上次是自动切换时）
  - Step4（≤768px JS+CSS）：body 添加 `.is-mobile` class，触发移动端侧边栏 overlay 布局
  - `switchView(mode, isAuto)` 新增 isAuto 参数，手动切换时重置 `autoSwitchedToCard`

- **修复移动端测试页无法访问**：VS Code 端口转发占用 3001 端口导致 426 响应，需在 VS Code PORTS 面板删除 3001 转发，或直接用 file:// 协议打开。
- **宽度恢复自动切回表格**：`applyViewByWidth()` ≥1200 分支移除 `autoSwitchedToCard` 条件，宽度恢复后始终切回表格视图（不再区分自动/手动切换）。

- **创建 5 个移动端布局测试页**（`public/test-mobile-1~5.html`）：
  1. 底部标签栏（4 tab: 全部/在库/已出库/临期 + 顶部搜索）
  2. 抽屉式菜单（hamburger + 左侧滑出导航面板 overlay）
  3. 分段控件（iOS 风格 pill segmented control + 分组列表）
  4. 悬浮按钮 FAB（右下角圆形+号按钮 + 设备图标头像 + 横向 chip tabs）
  5. 底部弹出面板（Bottom Sheet 筛选仓库/视图 + 底部固定操作栏）
