# 2026-06-24

- **列设置下拉菜单字体与整体不协调**：`.column-settings-menu` 纯白 `#fff` → `var(--bg-main)`，`border-radius`/`box-shadow` 改用 v4-linear 变量，`.column-settings-item` 显式 `font-family: var(--font-sans)` + `font-size: var(--text-sm)`，hover 用 `var(--primary-light)`。style.css → `?v=4`。
- **设备列表铺满屏幕剩余高度**：`.table-card` → `display: flex; flex-direction: column; height: 100%;`，`.table-card-header` → `flex-shrink: 0;`，`.table-responsive` → `flex: 1; overflow: auto; min-height: 0;`。`.list-view`/`.empty-state` 同步调整。去掉 listViewContainer inline padding。style.css → `?v=5`。
- **批量操作功能**（从生产版 port 到 v4-linear）：HTML 新增 `batch-toggle-btn` 按钮、`batch-toolbar` 工具栏、`batch-check-col` 列表头。JS 新增完整批量逻辑：`batchSelectMode`/`selectedDeviceIds` 状态、拖拽多选 `initDragSelect`、`toggleDeviceSelectDirect`、`toggleBatchSelectMode`/`updateBatchBtnUI`、`toggleDeviceSelect`/`toggleSelectAll`/`updateBatchSelectUI`、`renderBatchToolbarHTML`、`executeBatchDelete`（调用 `/api/devices/batch-delete`）。`renderTable` 批量模式下渲染复选框列 + `data-device-id` + 行选中样式。批量模式下表格行不可点击跳详情。CSS 新增 `.batch-toggle-btn`、`.batch-toolbar`、`.batch-select-all`、`.batch-delete-btn`、`.batch-check-col`/`.batch-checkbox`、`tr.batch-selected` 全部 v4-linear 风格。CSS/JS → `?v=6`。
- **设备列表铺满屏幕剩余高度**：`.table-card` → `display: flex; flex-direction: column; height: 100%;`，`.table-card-header` → `flex-shrink: 0;`，`.table-responsive` → `flex: 1; overflow: auto; min-height: 0;`（表格内部滚动而非整页滚动）。`.list-view` → `flex: 1; overflow-y: auto; align-content: start;`。`.empty-state` → `flex: 1; display: flex;` 居中。HTML 中 listViewContainer 去掉 inline `padding:16px`。style.css → `?v=5`。
- **统计概览筛选状态**：侧边栏 `.sidebar-stat-item` 新增 `.active` 类（`background: var(--sidebar-active)`），与仓库列表 active 风格统一。新增 `updateStatHighlight()` 函数管理选中态。修复原 `switchDeviceTab` 中错误选择器 `.stat-card`（实际不存在）和 inline style 写法，改为 class 驱动。`selectWarehouse` 重置 tab 时同步高亮，页面初始加载时默认高亮「设备总数」。CSS/JS → `?v=7`。
- **批量操作底部删除栏**：在 `.table-card` 底部新增 `.batch-footer-bar`（`flex-shrink: 0`），仅在批量模式且有选中项时显示。含「已选 N 项」计数和红色「批量删除」按钮。`renderBatchFooterBar()` 函数驱动显隐，在 `updateBatchSelectUI` / `toggleBatchSelectMode` 中同步调用。CSS/JS/HTML → `?v=8`。
- **全选后批量删除按钮不出现**：`toggleSelectAll()` 全选操作后只调了 `renderDevices()` 重绘表格行，未刷新工具栏/底部栏，导致两个删除按钮都不会渲染出来。修复：`toggleSelectAll()` 末尾追加 `updateBatchSelectUI()` + `renderBatchToolbarHTML()`。JS → `?v=9`。
- **移除批量操作底部栏**：用户要求只保留顶部工具栏的批量删除按钮。删除 HTML `#batchFooterBar`、CSS `.batch-footer-bar`/`-count`/`-delete` 整块、JS `renderBatchFooterBar()` 函数及其在 `toggleBatchSelectMode`/`updateBatchSelectUI` 中的调用。CSS/JS → `?v=9/10`。
- **导入错误提示"第X行"→"第X条"**：`server/index.js`（8处：文件导入4处 + 批量粘贴4处）和 `worker/index.js`（4处）中所有导入校验错误消息从"第X行"统一改为"第X条"，更贴近设备资产语境。
- **导入后仓库数量显示为0**：`doImport()`/`startBatchImport()` 中 `loadDevices()` 和 `loadWarehouses()` 都是 async 但未 await，存在竞态。`loadDevices()` → `updateWarehouseCounts()` 正确计算数量后，`loadWarehouses()` 从后端拉取数据直接覆盖 `warehouses`（无 `device_count`），导致渲染时数量为 0。修复：`loadWarehouses()` 在赋值 `warehouses` 后检查 `allDevices.length > 0`，如有则调用 `updateWarehouseCounts()` 重新计算。JS → `?v=11`。**随后发现 `?v=11` 的修改意外删除了 `const data = await r.json()` 这一行，导致仓库列表全部为空（数量全 0）——已修复，还原该行。JS → `?v=12`。
- **临期统计改为7天内**：`updateStats()` 从 30 天窗口（`30*86400000`）改为 7 天（`7*86400000`）。后端 `/api/devices/expiring` 同步添加 `DATEDIFF <= 7` 过滤（`server/index.js` + `worker/index.js`）。JS → `?v=13`。
- **临期提醒列表：默认5条，更多展开10条**：新增 `expiringDevicesAll`/`expiringShowAll` 状态变量。`loadExpiringDevices()` 默认只渲染前5条。新增 `toggleExpiringMore()` 切换5/全部。`renderExpiringList()` 控制「更多」按钮显隐和文案（"更多（共N条）"/"收起"）。HTML 新增 `#expiringMoreBtn` 按钮。CSS 新增 `.expiring-more-btn` 样式。同时将剩余天数危险阈值从 `<10` 改为 `<=2`（7天窗口下更合理）。CSS/JS/HTML → `?v=10/13`。
- **到期日期颜色分级**：新增 `getExpiryRemainingDays(dateStr)` / `getExpiryColorStyle(days)` / `formatExpiryBadge(dateStr)` 三个工具函数。颜色规则：≤7天红色、8-30天橙色（`--orange: #D97706`）、31-365天黄色、>365天绿色。CSS 新增 `:root --orange`/`--orange-bg` 变量和 `.expiry-badge` 通用样式。侧边栏/表格/卡片全部改用 `formatExpiryBadge()` 统一颜色。CSS/JS → `?v=11/14`。**橙黄不区分：`--warning` #B8860B → #CA8A04（亮金）、`--warning-bg` #FFFCF0 → #FEF9C3（明黄底）。CSS → `?v=12`**。**黄色背景太重：`--warning` #CA8A04 → #A68A00（暗金）、`--warning-bg` #FEF9C3 → #FFFDEB（极淡米黄，与其他淡色底统一）。CSS → `?v=13`**
- **备注弹窗从 alert 升级为富文本编辑器**：移植生产版 `remarkPreviewModal` 到 v4-linear，无 Bootstrap 依赖。HTML：新增 `.modal-overlay#remarkPreviewModal`（含设备名栏、完整富文本工具栏17按钮、contenteditable 编辑区）、色板选择器 `#colorSwatchPopover`、字体大小面板 `#fontSizePopover`、图片右键尺寸菜单 `#imgSizeContextMenu`、图片插入弹窗 `#imageInsertPopup`、隐藏 file input `#imagePickerFileInput`。CSS：新增 `.remark-toolbar`/`-btn`/`-divider`、`.remark-editor`、`.color-swatch-popover`/`-grid`/`-item`、`.font-size-popover`/`-item`、`.table-picker-popup`/`-grid`/`-cell`、`.img-size-context-menu`、`.img-upload-progress` 等全部 v4-linear 风格。JS：替换原 `alert()` 版 `showRemarkPreview`，新增完整富文本模块——`closeRemarkModal`/`updateRemarkPreviewStatus`/`saveRemarkFromPreview`、`encodeRichText`/`decodeRichText`/`decodeRichTextToSingleLine`、`execCmd`、表格插入 `showTablePicker`+`insertTableHtml`、色板 `SWATCH_COLORS`+`toggleColorPanel`+`applyColor`、字体 `toggleFontSizePanel`+`applyFontSize`、图片 `insertImageToRemark`+`handleImagePickerUpload`+`setupImagePaste`+`pasteAndUploadImage`+`convertImageUrl`+`insertHTMLAtCursor`、右键菜单 `setupImageContextMenu`+`setImageSize`，以及 IIFE 初始化（绑定 input/dblclick/contextmenu/paste 事件）。CSS/JS/HTML → `?v=14/15`。
- **仓库已有图片浏览器**：从生产版移植到 v4-linear。HTML：`imageInsertPopup` 升级为双按钮（上传新图片 + 仓库已有图片），新增 `.modal-overlay#imageBrowserModal`（含 `imageGridContainer` 网格容器、底部工具栏「选中未使用」「删除选中」「插入选中」按钮、选中计数、已用/未用图例）。CSS：新增 `.image-grid`/`-item` 5列网格（≤600px→3列）、`.select-check` 选中标记、`.delete-btn` hover 显示、`.usage-badge` 使用状态标签（绿色已用/红色未用）、`.image-empty-state` 空状态、`.usage-legend` 图例、`.image-picker-footer` 底部工具栏。JS：新增 `_imageTargetEditorId`/`_selectedImages`/`_existingImages` 状态变量，`insertImageToRemark` 同步设置 `_imageTargetEditorId`。新增完整图片浏览器函数——`openImageBrowser`/`closeImageBrowser`（v4-linear 模态）、`loadExistingImages`（`GET /api/images/list/:deviceId`）、`getUsedImageFilenames`（从编辑器 HTML 提取已引用的图片文件名）、`renderImageGrid`（5列网格含选中/删除/使用状态）、`toggleImageSelection`/`selectAllUnused`/`insertSelectedImages`（批量插入到编辑器光标位置）、`deleteSingleImage`/`deleteSelectedImages`（`DELETE /api/images/:deviceId/:filename`）、`updateImageSelectionUI`（按钮 disabled 联动）、`truncateFilename`/`formatFileSize` 辅助函数。CSS/JS/HTML → `?v=15/16`。
- **图片插入后状态指示器不变红**：`updateRemarkPreviewStatus()` 依靠 `input` 事件触发，但编程方式插入图片（上传替换/粘贴上传/仓库选中批量插入）不会触发 `input`，导致绿点不变。修复：在 `handleImagePickerUpload` 成功插入后、`pasteAndUploadImage` 成功插入后、`insertSelectedImages` forEach 结束后各加 `updateRemarkPreviewStatus()` 调用。JS → `?v=17`。
- **备注换行保存后重新打开不生效**：根因是 `decodeRichText` 把 `\n` → `<br>`，但 Chrome contenteditable 按 Enter 产生的是 `<div>段落</div>` 块结构，直接用 `<br>` 行内换行会导致 DOM 结构不匹配。修复：`decodeRichText` 改为按 `\n\n` 分割段落，每段包 `<div>`，段内 `\n` 才转 `<br>`；空段落生成 `<div><br></div>`。同时修复 `\\n`（literal 转义）→ 真实换行。JS → `?v=18`。
- **编辑设备弹窗增加图片预览、富文本编辑按钮、附件管理**：1) 图片预览：编辑已有设备时在弹窗中显示 5 列图片网格（`.modal-image-grid`），点击使用现有 `showImageFullscreen` 全屏查看，通过 `loadModalImages`（`GET /api/images/list/:deviceId`）加载。2) 富文本编辑按钮：备注标签旁新增「富文本编辑」按钮，调用 `openRichTextEditorFromModal()` 打开 remarkPreviewModal，保存时通过 `_remarkSourceMode === 'modal'` 回写到 `#frmRemark` textarea 而非直接保存到服务器；`showRemarkPreview`（表格点击）重置 `_remarkSourceMode = null` 恢复直接保存模式。3) 附件管理：移植 device.html 附件逻辑到编辑弹窗，在备注前新增附件列表（`.modal-attachment-list`/`-item`/`-icon`/`-info`/`-actions`/`.btn-attachment-delete`）、上传按钮、隐藏 file input `#attachmentFileInput`；函数：`formatFileSize`/`getAttachmentIconClass`/`loadDeviceAttachments`/`triggerUploadAttachment`/`handleAttachmentUpload`/`deleteAttachmentFile`（使用 `deviceModalId` 对应的 6 位码）。CSS 新增 `@keyframes spin`。新增设备不显示图片/上传按钮。CSS/JS/HTML → `?v=18/19`。
- **编辑弹窗重构：图片融入备注预览、移除独立图片栏**：1) 删除独立的「图片」5列网格区域、`loadModalImages`/`renderModalImageGrid` 函数、`.modal-image-grid`/`-item`/`-hover` CSS。2) 备注从 plain textarea 改为富文本预览区 `.modal-remark-preview.rich-text-content`（`decodeRichText()` 渲染，图片可双击全屏），隐藏 textarea `#frmRemark` 存原始值。`openRichTextEditorFromModal` 仍读取隐藏 textarea，`saveRemarkFromPreview`（modal 模式）回写 textarea + 更新 `#frmRemarkPreview` innerHTML。3) CSS 新增完整 `.rich-text-content` 样式——段落/列表/粗斜体/下划删除线/链接/图片（max-width:100%, zoom-in cursor, hover scale）/表格，`.modal-remark-preview`（空时 `::after` 显示"暂无备注"）。标签栏已存在（`id=frmTags`），输入框样式不变。CSS/JS/HTML → `?v=19/20`。
- **富文本编辑按钮右对齐 + 标签栏改用生产版 edit-row 布局**：1) `.modal-section-heading` 加 `justify-content: space-between`，「富文本编辑」按钮自动贴右。2) 标签从垂直 `form-group`（label + input）改为生产版横向 `edit-row`（grid 100px + 1fr）：`.edit-row` / `.edit-label` / `.edit-form-group` 全套 CSS 移植到 style.css，`openDeviceModal` 中 frmTags 行对应改为 `<div class="edit-row"><div class="edit-label">设备标签</div><div class="edit-form-group">...`。CSS/JS/HTML → `?v=20/21`。
- **标签栏升级为交互式 badge 系统（正式版设计）**：1) HTML：添加全局 `<datalist id="tagSuggestionsList">`。2) CSS：新增 `.tag-badge-editable`（inline-flex, primary-light 背景, primary-border 边框）+ `.tag-remove`（hover→danger 红）、`.modal-tag-input-row`（flex input + 按钮横排）全套 v4-linear 主题样式。3) JS：新增 `modalTags` 数组 + `initModalTags(device)`（解析 JSON/逗号两种格式初始化）+ `renderModalTagBadges()`（渲染 badge×删除）+ `addTagToDevice()`（回车或按钮触发，去重检查）+ `removeTagFromDevice(tag)` + `getDeviceTagsJSON()`（返回 JSON 字符串）+ `collectAllTagNames()`（遍历 allDevices 收集标签名）+ `updateTagDatalist()`（更新 datalist 建议选项）。`openDeviceModal` 中标签区域改为 badge 区 + input+「添加」按钮 + 回车事件；`saveDevice` 中 `frmTags` → `getDeviceTagsJSON()`。CSS/JS/HTML → `?v=21/22`。
- **表单间距调整（顶部底部不要贴太近）**：`.form-group` `margin-bottom` 16px→20px，`.form-label` `margin-bottom` 4px→6px；标签 badge 容器 inline `gap` 4px→6px、`margin-bottom` 8px→10px、`min-height` 24px→28px；`.modal-tag-input-row` `gap` 6px→8px、`margin-top` 8px→10px。CSS/JS → `?v=22/23`。
- **修复 .form-row 缺少底部间距导致相邻栏紧贴**：`.form-row` 无 `margin-bottom`，导致后面紧跟的 `.form-group`（如标签紧贴规格型号行）或 `.form-row` 之间间距为 0。添加 `.form-row { margin-bottom: 20px }` + `.form-row:last-child { margin-bottom: 0 }` 安全网。CSS → `?v=23`。
- **修复设备详情页内容显示不全**：根因是 `style.css` 全局 `body { overflow: hidden; height: 100vh; }` 在 `device.html` 中阻止页面滚动。`device.html` `<style>` 中新增 `body { overflow: auto; height: auto; }` 覆盖，同时移除 `.device-page` 的 `overflow-x: hidden`。
