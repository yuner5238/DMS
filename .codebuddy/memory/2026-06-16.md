# 2026-06-16

## 拖拽多选 + 设备分区样式重构

### 变更记录
- 2026-06-16: 实现拖拽多选设备 —— 批量模式下 pointerdown 在设备行上启动拖选，pointermove 划过即选中，pointerup 结束并更新 UI；5px 阈值区分点击与拖拽；拖拽后取消 click 事件防止误入详情页；`toggleDeviceSelectDirect` 直接操作 DOM 避免全量渲染
- 2026-06-16: 设备分区样式改为直角无缝连接 —— 取消 `.device-section-header` / `.device-section-body` / `.batch-toolbar` 的 border-radius；`margin-bottom: 0` 使在库与已出库上下紧贴；绿线 (#107c10) 区分在库、黄线 (#eaa300) 区分已出库；同步修改 HTML 内联样式和移动端响应式；清理批量模式 CSS 中已冗余规则
- 2026-06-16: 修复分区 header hover/active 缩放遮挡边框 —— 去掉 `transform: scale(1.005)` 和 `scale(0.998)`，改为背景色变化（hover: #f2f3f5, active: #e8e9eb），避免 layout shift 和边框被覆盖
- 2026-06-16: 重构操作工具栏 —— 将排序、搜索、批量操作、视图切换、添加设备按钮从 `.page-header` 移到 `#batchToolbarContainer`（类名 `action-toolbar-wrapper`）；主工具栏始终显示（flex wrap），批量模式时下方追加 `#batchControlsBar` 显示全选/删除；移动端隐藏桌面工具栏；CSS 双重边框问题已修复（wrapper 内 .batch-toolbar 仅保留顶部分隔线）
- 2026-06-16: 工具栏左右分组排版 —— 搜索框占左（flex:1），其余控件包裹在 `.toolbar-actions` 靠右（margin-left:auto），顺序：排序→视图切换→批量操作→添加设备；去掉 flex-wrap 避免换行
- 2026-06-16: 统一卡片容器解决割裂感 —— 新增 `.content-card` 包裹工具栏+设备列表，单一外边框 + `border-radius: 8px`；`.action-toolbar-wrapper` 去掉独立边框仅保留 `flex-shrink:0`
- 2026-06-16: 邮箱风格全面重构 —— 参考邮箱客户端布局：`.content-card` 去圆角变扁平矩形面板；工具栏加 `border-bottom` 作为顶栏分隔线；分区标题改为轻量分组头（浅色底 #f7f8fa + 底部细线 + 左侧彩色竖条区分在库/出库）；设备项从卡片式（圆角+margin+边框+悬浮阴影）改为行列表式（无圆角无margin无边框仅底部细线 #f0f0f0 + hover 背景色变化）；批量选中样式同步简化；所有断点 device-item margin 归零
- 2026-06-16: 修复 Worker 缺少 batch-delete 路由导致 404 —— `worker/index.js` 新增 `POST /api/devices/batch-delete` 路由和 `batchDeleteDevices()` 函数；Pages Functions 中间件代理 `/api/*` 到 Worker 但 Worker 未实现该端点
- 2026-06-16: 修复 Worker S3 上传 500 错误 —— `signS3Request()` 中 `canonicalUri = s3Url.pathname` 会解码 %20 为空格，但实际 HTTP 请求发送编码后的 URL，导致 AWS V4 签名校验失败。改为 `pathname.split('/').map(encodeURIComponent).join('/')` 重新编码路径段
- 2026-06-16 16:58: **修复 S3 签名 double encoding 导致上传 502** —— 上一项修复引入回归：调用方已 `encodeURI(s3Key)` 构造 URL（pathname 中已有 %20），`signS3Request` 再 `encodeURIComponent` 导致 `%20` → `%2520`，签名与实际请求路径不匹配，S3 返回 403 → Worker 返回 502。修复为 `decodeURIComponent(seg)` 先解码再 `encodeURIComponent` 确保单次编码
- 2026-06-16 16:53: 版本号从 v2026.6.5 更新为 v2026.6.16.16.53 —— `public/index.html` 侧边栏标题处
