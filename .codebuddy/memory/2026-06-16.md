# 2026-06-16

## 拖拽多选 + 设备分区样式重构

### 变更记录
- 2026-06-16: 实现拖拽多选设备 —— 批量模式下 pointerdown 在设备行上启动拖选，pointermove 划过即选中，pointerup 结束并更新 UI；5px 阈值区分点击与拖拽；拖拽后取消 click 事件防止误入详情页；`toggleDeviceSelectDirect` 直接操作 DOM 避免全量渲染
- 2026-06-16: 设备分区样式改为直角无缝连接 —— 取消 `.device-section-header` / `.device-section-body` / `.batch-toolbar` 的 border-radius；`margin-bottom: 0` 使在库与已出库上下紧贴；绿线 (#107c10) 区分在库、黄线 (#eaa300) 区分已出库；同步修改 HTML 内联样式和移动端响应式；清理批量模式 CSS 中已冗余规则
- 2026-06-16: 修复分区 header hover/active 缩放遮挡边框 —— 去掉 `transform: scale(1.005)` 和 `scale(0.998)`，改为背景色变化（hover: #f2f3f5, active: #e8e9eb），避免 layout shift 和边框被覆盖
- 2026-06-16: 重构操作工具栏 —— 将排序、搜索、批量操作、视图切换、添加设备按钮从 `.page-header` 移到 `#batchToolbarContainer`（类名 `action-toolbar-wrapper`）；主工具栏始终显示（flex wrap），批量模式时下方追加 `#batchControlsBar` 显示全选/删除；移动端隐藏桌面工具栏；CSS 双重边框问题已修复（wrapper 内 .batch-toolbar 仅保留顶部分隔线）
- 2026-06-16: 工具栏左右分组排版 —— 搜索框占左（flex:1），其余控件包裹在 `.toolbar-actions` 靠右（margin-left:auto），顺序：排序→视图切换→批量操作→添加设备；去掉 flex-wrap 避免换行
- 2026-06-16: 统一卡片容器解决割裂感 —— 新增 `.content-card` 包裹工具栏+设备列表，单一外边框 + `border-radius: 8px`；`.action-toolbar-wrapper` 去掉独立边框仅保留 `flex-shrink:0`
- 2026-06-16: 邮箱风格全面重构 —— 参考邮箱客户端布局：`.content-card` 去圆角变扁平矩形面板；工具栏加 `border-bottom` 作为顶栏分隔线；分区标题改为轻量分组头（浅色底 #f7f8fa + 底部细线 + 左侧彩色竖条区分在库/出库）；设备项从卡片式（圆角+margin+边框+悬浮阴影）改为行列表式（无圆角无margin无边框仅底部细线 #f0f0f0 + hover 背景色变化）；批量选中样式同步简化；所有断点 device-item margin 归零
- 2026-06-16: 修复 Worker 缺少 batch-delete 路由导致 404 —— `worker/index.js` 新增 `POST /api/devices/batch-delete` 路由和 `batchDeleteDevices()` 函数；Pages Functions 中间件代理 `/api/*` 到 Worker 但 Worker 未实现该端点
- 2026-06-16: 修复 Worker S3 上传 500 错误 —— `signS3Request()` 中 `canonicalUri = s3Url.pathname` 会解码 %20 为空格，但实际 HTTP 请求发送编码后的 URL，导致 AWS V4 签名校验失败。改为 `pathname.split('/').map(encodeURIComponent).join('/')` 重新编码路径段
- 2026-06-16 16:58: **修复 S3 签名 double encoding 导致上传 502** —— 上一项修复引入回归：调用方已 `encodeURI(s3Key)` 构造 URL（pathname 中已有 %20），`signS3Request` 再 `encodeURIComponent` 导致 `%20` → `%2520`，签名与实际请求路径不匹配，S3 返回 403 → Worker 返回 502。修复为 `decodeURIComponent(seg)` 先解码再 `encodeURIComponent` 确保单次编码
- 2026-06-16 16:53: 版本号从 v2026.6.5 更新为 v2026.6.16.16.53 —— `public/index.html` 侧边栏标题处
- 2026-06-16~17: **S3 签名彻底重构** —— 之前两次修复 (500→502→500) 均因 URL.pathname 编码行为在不同运行时不一致。最终方案：`signS3Request(env, method, s3Url, body, contentType, objectKey)` 新增 `objectKey` 参数，直接接收原始未编码 s3Key，从 `/${bucket}/${objectKey}` 独立计算 canonicalUri；fetch 请求 URL 仍使用 `encodeURI(s3Key)` 确保合法 URL。6 个 S3 函数统一此模式。commit `e197da8`
- 2026-06-17 12:28: **payloadHash 改用 emptyHash 替代 UNSIGNED-PAYLOAD** —— cstcloud 等部分 S3 兼容服务不支持 UNSIGNED-PAYLOAD，HEAD 验证请求被拒导致 500。改用 SHA-256(空)=`e3b0c44...`；同时增强 catch 块返回 `${err.name}: ${err.message}` 和 stack。commit `acdb9f3`
- 2026-06-17 13:50: **HEAD 验证改为非阻塞** —— PUT 上传成功但 cstcloud HEAD 仍失败（可能是 cstcloud 不支持 HEAD），改为仅记录日志不阻断上传。commit `f99958e`
- 2026-06-17 14:04: **修复附件列表高度被压缩** —— `public/index.html` 中 `#deviceAttachmentsList` inline style 有 `overflow:hidden` 覆盖了 CSS 的 `overflow-y: auto`，内容超出 max-height 160px 后无法滚动被裁切。移除内联 overflow:hidden。commit `435239c`
- 2026-06-17 14:25: **修复 .attachment-item 被 flex 容器挤压变形** —— 多项附件在 `flex-direction: column` 容器中因缺少 `flex-shrink: 0` 被压缩至 `max-height: 160px` 内，文字图标挤成一团。加 `flex-shrink: 0` + `min-height: 44px`。commit `8de94ab`
- 2026-06-17 15:00: **修复附件删除/代理编码不一致导致删除失败** —— 根因：uploadAttachment 用 `encodeURIComponent(baseName)` 生成文件名（含 `%`），前端 `deleteAttachmentFile` 又做 decode→re-encode 产生双重编码，Worker 收到的 URL path 与 S3 实际 key 不匹配。三处修复：(1) uploadAttachment 不再用 `encodeURIComponent`，仅过滤控制字符 (2) 前端直接传 encodedFilename 不再二次编码 (3) Worker 的 deleteAttachment/deleteImage/proxyImage/proxyAttachment 统一 `decodeURIComponent(filename)` 还原真实 S3 key。commit `3327ddb`
- 2026-06-17 15:08: **修复 S3 URL 构造 encodeURI 二次编码旧文件名** —— 上一轮 fix 仍未解决：Worker 中 `encodeURI(s3Key)` 把旧文件已有的 `%` 编码成 `%25`，实际 HTTP 请求的 S3 URL 与对象 key 不匹配导致 401。新增 `safeEncodeS3Path()` 对每段做 decode→re-encode 归一化，替换全部 6 处 `encodeURI(s3Key)`（uploadAttachment/uploadImage/deleteAttachment/deleteImage/proxyAttachment/proxyImage）。commit `0d96a90`
- 2026-06-17 15:20: **device.html deleteAttachmentFile 同步 index.js** —— device.html 仍使用旧版 decode→re-encode 实现，已同步为直接传 encodedFilename。两文件均加 `console.log('[deleteAttachmentFile] called', ...)` 用于调试。commit `291a5d2`
- 2026-06-17 15:31: **本地版附件列表 displayName 乱码修复** —— 根因：云端 Worker 的 `parseS3ListAttachmentsXml` 对 displayName 有 `decodeURIComponent` 兼容旧编码文件，但本地版 `server/index.js:1343` 缺失此处理，导致旧文件（上传时用 `encodeURIComponent` 编码过文件名）在本地版显示为 `%E7%A7%BB%E6%A4%8D` 等乱码。修复：`server/index.js` 附件列表加 `try { displayName = decodeURIComponent(displayName); } catch (_) {}`。commit `3da4d49`
- 2026-06-17 15:36: **feat: 删除按钮 spinner + 上传/删除 console.log** —— 删除按钮 onclick 改为传 `this` 引用，点按后按钮内显示 Bootstrap spinner-border 并 disable；上传成功 `console.log('[upload] 上传成功:', filename)`；删除成功 `console.log('[delete] 删除成功:', filename)`；覆盖 index.js 和 device.html 的附件上传/删除函数。commit `2008d83`
- 2026-06-17 15:50: **移动端自动切换列表视图 + 标签禁止换行** —— (1) 视图恢复逻辑：移动端 (<=768px) 强制 `list` 视图忽略 localStorage 偏好，桌面端优先用户选择。(2) `.name-tags-row` 和 `.status-tags-row` 改为 `flex-wrap: nowrap; overflow: hidden`，`.tag-badge` 加 `white-space: nowrap`，防止长标签换行导致设备名从一行变两行。修改 index.css、index.html 内联样式、device.html。commit `5a18653`
