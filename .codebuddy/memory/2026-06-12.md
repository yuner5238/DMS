# 2026-06-12

## 设备列表批量多选 + 批量删除

### 修改文件
- `server/index.js` — 新增 `POST /api/devices/batch-delete` 接口
- `public/js/index.js` — 批量选择功能（始终显示复选框，无需切换模式）
- `public/css/index.css` — 批量操作工具栏/复选框/选中高亮样式

### 实现要点（最终版本）
- 每个设备行前面**始终显示**一个勾选框（表格视图和列表视图均支持）
- 顶部显示批量工具栏：全选复选框 + "已选 N 项" + "批量删除"按钮
- 点击行仍跳转到设备详情，点击复选框切换选中状态（互不影响）
- 全选/取消全选作用于当前列表所有可见设备
- 批量删除前弹出确认对话框
- 选中行蓝色高亮

### 变更记录
- 2026-06-15: 改为搜索栏旁的专用"批量操作"按钮，点击显示勾选框/工具栏，再次点击退出并清空选择，平时不显示勾选框
- 2026-06-15: 修复 CSS 布局错乱 —— batchToggleBtn 误用 filter-btn 类导致被强制 30x30px；改为独立样式，同时给搜索框加了宽度约束
- 2026-06-15: 修复勾选框闪烁消失 —— updateBatchBtnUI 用 innerHTML 重建按钮子节点导致问题；改为直接修改 className + textContent，并加 stopPropagation 防止冒泡
- 2026-06-15: 修复表格视图下批量勾选框不显示 —— applyColumnVisibility() 用 d-none 隐藏了不在 COLUMN_DEFS 中的 batch-check 列；在循环中跳过 batch-check 列
- 2026-06-15: 重构批量工具栏位置 —— 从"在库设备"区域内移到标题栏下方独立容器 `#batchToolbarContainer`，提取 `renderBatchToolbarHTML()` 公共函数，卡片/表格视图统一调用；toolbar CSS 改为全圆角独立卡片样式
- 2026-06-15: 批量模式统一面板设计 —— toolbar/在库/已出库三个框架无缝连接为整体；通过 `#deviceList.batch-mode` 移除分区间隙、取消独立边框、统一8px外框圆角；toolbar顶部圆角 + 末尾分区底部圆角形成完整卡片
