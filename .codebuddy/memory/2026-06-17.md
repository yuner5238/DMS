# 2026-06-17

- 16:23: **列表视图备注列禁止换行** —— 列表视图中备注栏原本有 hover 换行规则（`.device-details .detail-item:hover .detail-value { white-space: normal; word-break: break-all; }`），导致备注溢出时换行显示。新增 `.device-details .detail-item.remark:hover .detail-value { white-space: nowrap !important; word-break: normal !important; }` 覆盖 hover 行为，确保备注始终单行+省略号。修改 index.css、index.html。
